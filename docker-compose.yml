version: '3.9'

services:
  api:
    build: .
    ports:
      - "7860:7860"
    env_file:
      - .env
    volumes:
      - ./herhealthai.db:/app/herhealthai.db
      - ./pcos_ml_model.pkl:/app/pcos_ml_model.pkl
      - ./model_metrics.json:/app/model_metrics.json
